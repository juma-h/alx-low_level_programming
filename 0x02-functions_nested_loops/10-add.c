#include <stdio.h>
#include "main.h"
/**
 * add - the adition of two integers
 * @a: first nteger
 * @b: second integer
 * Return: the result of the adition of two integers
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
