#include <stdio.h>
#include "main.h"
/**
 * main - function that prints the programs name
 * @argc: contains the arguments counts
 * @argv: has the argument values
 * Return: 0
*/
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
